/*
 * ポインタ変数a, bを付け取り、a, bの中身を入れ替えるswap()関数を作成しなさい。
 * またキーボードから整数a, bを受け取り、swap()関数を呼び出して、a, bの中身を入れ替えなさい。
 * ただし、入れ替える前と後のどちらも表示すること
 *
 * [入力例]
 * 6
 * 3
 *
 * [出力例]
 * a = 6, b = 3
 * call swap()
 * a = 3, b = 6
 */
#include <stdio.h>

int main(void){
    return 0;
}
  
 