A30
lesson2exam1
l22　バックスラッシュ
l23　%と表示したいときは"%%"と書きます

lesson2exam2
l3,4　実数を扱う変数を宣言するときはdoubleまたはfloat

lesson4exam3
l18　複数の条件を同時に判定する場合は&&や||を用います
	(条件1 && 条件2) --> 条件1と条件2の両方が真である場合



B01
lesson2exam1
l20　%と表示したいときは"%%"と書きます

lesson5exam2
l21　maxの初期化が抜けてます


B04
L3e2
C言語で複数の条件式を用いる場合、式と式の間に&&(アンド、かつ)、||(オア、または)という演算子を用います。Pythonなどでは不要ですよね。

L4e1l17
出力するのは*です。

L4e2
10の倍数ごとに改行する必要があります。
if(i%10==0)かfor文を二つ用いましょう。

L4e4
l18
0以下は(n<=0)で判定します。以上以下未満より大きい小さい、ややこしいですが意味が違うので気を付けましょう。
l27
return 0;はmain関数(プログラム)の終了を意味します。
ループから抜け出す時にはbreak;を用いた方がいいです

L5e1
l21 10回ループする場合は0→9、または1→10で10回です。
あと112.3になっています。


B05
L1e2
l10 6と直接書くのではなく1+2+3のように記述するのが正解です。わかんないよね、ソースコードなんてね。

L2e2
l13,15 double型の演算結果を格納する変数は同じくdouble型でなければなりません。
int型に格納してしまうと小数点以下が切り捨てられてしまいます。（意図的に使用する場合もある）

L6e1
l30~ 初期化の値が違います。
あと構造体の変数名の名前の順番も違います。



B10
L2e1
剰余（あまり）も出力してください。（演算子は%です。）

L3e3
else ifを用いる場合それまでのif文で真と判定されたものは既に省かれているので条件式は必要ありません。
課題の場合二つ目のelse ifは、(a>=70)のみで問題ありません。以下も同様です。

L4e2
nは入力しないよ。あと見にくいから空白入れたほうがいいよ。


B12
L1e2
l10 ごめん、1,2,3です。

L3e3
else ifを用いる場合それまでのif文で真と判定されたものは既に省かれているので条件式は必要ありません。
課題の場合二つ目のelse ifは、(a>=70)のみで問題ありません。以下も同様です。

L5e2
配列に格納してから処理するという課題です。
合計値の変数は=0、最大値、最長値の変数は=(一つ目の値)を初期化、格納してから処理を行います。
何も入っていない変数を演算に使うと（+=なども）正しく動作してくれません。


B15
L3e2
l15 20以上80"未満"なので80は含めません。(a<80)

L5e2
l20 変数aを初期化していないため、a=a+bは定義されていません。
l33 平均値は実数となる場合があるため、書式指定子は%fまたは%lfを使いましょう。

L6e1
sumとaverageもstudent_tと合わせて一つの構造体として扱わなければならないようなのですが、
この問題の文章もおかしい場所があるのでで正解としました。


B19
L1e1
l11 この問題だけ最後の改行が必須となっています。

L2e1
bには5を初期化してください。

L5e1
l21 不等号が逆です。(i<10)

L5e2
l17 平均値を格納するavgは実数型で宣言してください。
l18,19 最小値、最大値はまず入力された一つ目の値を格納してください。
入力された値に0がなくても、0が出力される可能性があります。

L6e1
l32 値が一つ抜けてます。

L7e1
get_absolute()関数は絶対値を返すだけの関数です。
二つの値はキーボードから入力するようにしてください。

B06
L4e3
l22 forの増分処理は先頭に来る度に行われます。
そのため、ループの先頭ごとにiを1にするには、ここをi=0としなければなりません。

L4e4
l17 0以下の値を判定するためにはn<=0としなければなりません。
以上以下未満より大きい小さい、ややこしいですが意味が違うので気を付けましょう。

L5e2
惜しい！0~100までの範囲という指定はないです。
最小値、最大値はまず入力された一つ目の値を格納してください。
それと、意味の異なる値を格納する場合は別の変数を用いたほうが良いです。
実数型変数avgをつくるか、printfの中で直接sum/10を入れましょう。

L7e1
get_absolute()関数は絶対値を返すだけの関数です。
abs()と同じものをユーザ関数として作ってください。

